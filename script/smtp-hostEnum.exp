#!/usr/bin/expect

set domain [lindex $argv 0]
set mail_exchanger "mx"

spawn nslookup -type=$mail_exchanger $domain
expect "$ "

set output $expect_out(buffer)

spawn "ls"
expect "$ "

spawn echo $output
expect "$ "

# expect eof